@model IEnumerable<ApplicationUser>

@{
    ViewBag.Title = "users List";
}

<div class="card-group">
    @foreach (var user in Model)
    {
        @if (String.Equals(User.Identity.Name,user.Email))
        {
            continue;
        }
        <div class="card m-4" style="max-width:305px; min-width:305px;">
            <img class="card-img-top" src="~/images/noimage.jpg" />
            <div class="card-header">
                <h3>@user.Name</h3>
            </div>
            <div class="card-footer text-center">
                <a asp-controller="home" asp-action="details" asp-route-id="@user.Id" class="btn btn-primary">View</a>
                <a asp-controller="home" asp-action="edit" asp-route-id="@user.Id"
                   class="btn btn-primary m-1">Edit</a>
                <a asp-controller="home" asp-action="delete" asp-route-id="@user.Id" class="btn btn-danger">Delete</a>
            </div>
        </div>
    }
</div>


